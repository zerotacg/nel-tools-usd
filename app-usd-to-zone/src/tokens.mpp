module;
#include <pxr/base/tf/token.h>

export module nel_tools.usd.usd_to_zone.tokens;

export namespace nel_tools::usd::usd_to_zone::tokens
{
    inline const struct
    {
        const pxr::TfToken root = pxr::TfToken("root");
    } Tokens;
}
