module;
#include <string>

export module nel_tools.usd.usd_to_mesh.convert.material.TextureSettings;

export namespace nel_tools::usd::usd_to_mesh::convert::material
{
    struct TextureSettings
    {
        const bool removeTextureFilePath;
    };
}
